[pytest]
log_cli = true
log_cli_level = DEBUG
addopts = --order-scope=module
markers =
    integration: cross-layer scheduler tests
    live: requires PostgreSQL, RabbitMQ, and running scheduler services
